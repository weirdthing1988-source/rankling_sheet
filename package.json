{
  "name": "rankling-cohort-sheet",
  "version": "1.2.0",
  "private": true,
  "description": "Static Rankling cohort character sheet for D&D, deployed with Cloudflare Workers Static Assets.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
